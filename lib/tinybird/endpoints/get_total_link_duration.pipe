NODE endpoint
SQL >
    %
    SELECT
        SUM(duration) AS sum_duration
    FROM page_views
    WHERE
        linkId = {{ String(linkId, required=True) }}
        AND time >= {{ Int64(since, required=True) }}

TYPE ENDPOINT
