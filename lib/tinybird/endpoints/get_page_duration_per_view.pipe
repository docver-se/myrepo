NODE endpoint
SQL >
    %
    SELECT
        pageNumber as page,
        sum(duration) as duration
    FROM
        page_views
    WHERE
        viewId = {{ String(viewId, required=True) }}
    GROUP BY
        pageNumber
    ORDER BY
        pageNumber ASC

TYPE ENDPOINT
