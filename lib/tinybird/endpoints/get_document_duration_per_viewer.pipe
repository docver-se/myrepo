NODE endpoint
SQL >
    %
    SELECT
        argMax(linkId, time) as link_id,
        sum(duration) as total_duration
    FROM page_views
    WHERE
        documentId = {{ String(doc_id, required=True) }}
    GROUP BY
        viewId

TYPE ENDPOINT
