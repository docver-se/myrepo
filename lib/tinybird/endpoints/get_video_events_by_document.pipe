NODE endpoint
SQL >
    %
    SELECT
        *
    FROM
        video_views
    WHERE
        document_id = {{ String(documentId, required=True) }}
    ORDER BY
        timestamp ASC




TYPE ENDPOINT
